{
  "name": "whatsapp-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "apt-get update && apt-get install -y chromium-browser && rm -rf /var/lib/apt/lists/*"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.29.0",
    "qrcode-terminal": "^0.12.0",
    "axios": "^1.7.0"
  },
  "engines": {
    "node": "18.x"
  }
}
